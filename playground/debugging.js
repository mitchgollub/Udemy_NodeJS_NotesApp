var person = {
    name: 'Mitch'
};

person.age = 27;

debugger;

console.table(person);

/// Commands
// node inspect <filename>
// n = NEXT
// c = CONTINUE
// repl = Read-Eval-Print-Loop (REPLACE MODE)